services:
  mysql:
    image: mysql
    ports:
      - "3306:3306"
    expose:
      - "3306"
    environment:
      - MYSQL_PASSWORD=
      - MYSQL_DATABASE=meupet
      - MYSQL_ROOT_PASSWORD=ThePassword
    volumes:
      - "./conf.d:/etc/mysql/conf.d:ro"
